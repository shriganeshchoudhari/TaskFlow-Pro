// TaskDetailPage — Phase 3 placeholder
import { Box, Typography, Paper } from '@mui/material';
export default function TaskDetailPage() {
  return (
    <Box>
      <Typography variant="h4" fontWeight={700} mb={3}>Task Detail</Typography>
      <Paper sx={{ p: 6, textAlign: 'center', color: 'text.secondary' }}>
        <Typography variant="h6">Task Detail coming in Phase 3</Typography>
      </Paper>
    </Box>
  );
}
